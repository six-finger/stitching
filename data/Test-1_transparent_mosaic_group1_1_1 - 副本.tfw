# 提供仿射变换参数，来将图像像素坐标映射到地理坐标系统中的位置

0.039130000000          # a1
0                       # a2
0                       # a3
-0.039130000000         # a4
418953.430580000044     # tx
4156275.454900000244    # ty

# 如果给定一个图像像素坐标为（x, y），则可以计算出该像素在地理坐标系统中的准确位置
# 地理X坐标 = (x * tfw[0]) + (y * tfw[1]) + tfw[4] 
# 地理Y坐标 = (x * tfw[2]) + (y * tfw[3]) + tfw[5] 
